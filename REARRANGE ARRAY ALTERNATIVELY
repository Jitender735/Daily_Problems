class Solution{
public:

long long max=arr[n-1]+1,start=0,end=n-1;
for(long long i=0;i<n;i++)
{
if(i%2==0)
arr[i]+=(arr[end--]%max)*max;
else
arr[i]+=(arr[end--]%max)*max;

for(long long i=0;i<n;i++)
arr[i]/=max;
}
}
